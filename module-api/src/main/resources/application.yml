spring:
  config:
    activate:
      on-profile: local
  application:
    name: module-api
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

server:
  port: 6001
